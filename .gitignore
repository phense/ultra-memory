__pycache__/
*.pyc
.pytest_cache/
.venv/
*.db
*.db-wal
*.db-shm
*.db-journal
.DS_Store

# Internal audit artifacts — process notes that embed local /Users paths; never
# part of the public release (removed from tracking 2026-06-03, BACKLOG §5.2.2).
docs/audit/

# The numbered engineering backlog is the maintainer's private working doc (internal
# process notes + local paths); it stays on disk but out of the public repo. The
# public-facing roadmap lives in the README "Status" section. (untracked 2026-06-04)
/BACKLOG.md

# Agent working-norms / Claude Code guidance for developing on this engine —
# maintainer's private working doc, kept on disk but out of the public repo
# (the public contributor guide is CONTRIBUTING.md + docs/12-contributing.md).
/CLAUDE.md
